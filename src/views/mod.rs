//! Settings views for SystemSettings.
//!
//! One module per settings page. The sidebar lives in `wifi.rs` for now
//! (single Wi-Fi/WLAN category); it moves to its own module once a second
//! category exists.

pub mod wifi;
